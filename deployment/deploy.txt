1.docker构建镜像并push
./build

2.首先，运行该脚本生成证书和私钥 secret 信息：

./deployment/webhook-create-signed-cert.sh  --namespace jiexun-test


3.创建 deployment 和 service 对象
kubectl create -f deployment/service.yaml -n  jiexun-test
kubectl create -f deployment/deployment.yaml -n jiexun-test


4.配置 webhook MutatingWebhookConfiguration
1）生成deployment/mutatingwebhook-ca-bundle.yaml文件
cat ./deployment/mutatingwebhook.yaml | ./deployment/webhook-patch-ca-bundle.sh > ./deployment/mutatingwebhook-ca-bundle.yaml

2）创建MutatingWebhookConfiguration资源
kubectl create -f deployment/mutatingwebhook-ca-bundle.yaml -n jiexun-test


5.设置namespace标签 namespaceSelector

kubectl label namespace jiexun-test jiexun-test-key=jiexun
kubectl label namespace jiexun-test jiexun-test-key-

6.测试Mutating回调
kubectl create -f deployment/test-secret.yaml




读这样一个configmap: 然后 有 monitoring-platform-access 的 namespace里所有object打
dce.daocloud.io/app=cloudera-cdp-console
标签


叫 mpala-1618238341-26vs 的namespace打
dce.daocloud.io/app=cloudera-cdp-impala
标签


apiVersion: v1
kind: ConfigMap
metadata:
  name: label-matches
  namespace: cdp-customizer
data:
  label/monitoring-platform-access: "dce.daocloud.io/app=cloudera-cdp-console"
  name/impala-1618238341-26vs: "dce.daocloud.io/app=cloudera-cdp-impala"


